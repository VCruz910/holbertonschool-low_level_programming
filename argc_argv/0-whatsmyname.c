#include "main.h"
/**
 * main - Prints its name, followed by a new line.
 * @argc: Variable for the number of command line.
 * @argv: Array for the Command Line argument.
 * Return: 0 (Success)
 **/
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
